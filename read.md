# Chrome 书签整理插件使用教程

这是一个本地优先的 Chrome 书签管理插件，用来扫描书签、识别重复项、生成分类建议，并在清理前自动备份。

## 主要功能

- **重复清理**：自动识别标题和规范化 URL 完全一致的低风险重复书签。
- **人工确认**：标题不同但 URL 相近的书签只展示为建议，不自动删除。
- **自动分类**：优先匹配已有文件夹，再使用标准分类兜底。
- **备份导出**：支持插件内备份，并导出 JSON / HTML 备份文件。
- **安全恢复入口**：当前版本会记录恢复请求并创建安全备份，不会直接重建整个真实书签树。

## 安装依赖

在项目根目录执行：

```powershell
npm install
```

## 构建插件

```powershell
npm run build
```

构建完成后，Chrome 插件文件会生成在：

```text
C:\Users\33002\Desktop\bookmark\dist
```

## 加载到 Chrome

1. 打开 Chrome。
2. 访问 `chrome://extensions`。
3. 打开右上角的 **开发者模式**。
4. 点击 **加载已解压的扩展程序**。
5. 选择项目里的 `dist` 文件夹：

```text
C:\Users\33002\Desktop\bookmark\dist
```

加载完成后，可以点击插件图标，再点击 **打开管理页**。

## 使用流程

### 1. 先备份

进入插件管理页后，打开 **备份恢复** 页签，点击 **立即备份**。

建议在第一次整理真实书签前，同时点击：

- **导出 JSON**
- **导出 HTML**

HTML 备份可以作为浏览器通用书签备份文件保存。

### 2. 查看概览

打开 **概览** 页签，可以看到：

- 书签总数
- 文件夹总数
- 重复组数量
- 分类建议数量

### 3. 清理重复书签

打开 **重复清理** 页签。

插件会把重复项分成两类：

- **可自动清理**：标题和规范化 URL 完全一致，插件会保留一个并删除多余项。
- **需要人工确认**：URL 相同或相近但标题不同，只展示，不自动删除。

点击 **备份并自动删除 X 个完全重复项** 后，插件会先创建备份，再删除低风险重复项。

### 4. 查看分类建议

打开 **自动分类** 页签。

插件会根据书签标题、URL 和已有文件夹生成建议。当前版本主要用于预览分类结果，后续可以继续扩展为批量移动确认流程。

### 5. 导出备份

打开 **备份恢复** 页签，可以导出最近一次备份：

- **导出 JSON**：保留完整结构和插件需要的元数据。
- **导出 HTML**：适合浏览器书签管理器导入。

## 推荐安全做法

第一次整理真实书签前，建议按顺序执行：

1. 在 Chrome 自带书签管理器中导出一份原始备份。
2. 在插件中点击 **立即备份**。
3. 在插件中导出 JSON 备份。
4. 再执行重复清理。

## 开发验证

运行完整验证：

```powershell
npm test
npm run typecheck
npm run build
```

当前测试覆盖：

- URL 规范化
- 重复书签识别
- 分类建议
- 书签树索引
- 备份序列化
- 操作日志

## 注意事项

- 插件只针对 Chrome Manifest V3。
- 当前版本默认本地处理书签，不上传数据。
- 自动删除只针对低风险完全重复项。
- 恢复功能当前是保守流程，不会直接删除并重建真实书签树。
